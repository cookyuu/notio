spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.notio: DEBUG
    org.springframework.web: DEBUG

notio:
  auth:
    mail:
      delivery-mode: ${NOTIO_AUTH_MAIL_DELIVERY_MODE:LOG}
  connection:
    security:
      credential-encryption-key: bG9jYWwtZGV2LWtleS1jaGFuZ2UtbWUtaW4tcHJvZDE
